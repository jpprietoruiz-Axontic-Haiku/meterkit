# Not needed locally: the Vite proxy (vite.config.ts) forwards API calls
# to http://localhost:3000.
#
# In production (Vercel), point it to the API's public URL on Railway:
VITE_API_BASE_URL=https://your-api.up.railway.app
