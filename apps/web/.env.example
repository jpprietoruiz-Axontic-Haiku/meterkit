# En local no hace falta: el proxy de Vite (vite.config.ts) reenvia las
# llamadas a la API a http://localhost:3000.
#
# En produccion (Vercel), apunta a la URL publica de la API en Railway:
VITE_API_BASE_URL=https://tu-api.up.railway.app
